<!DOCTYPE html>
<html>
<head>
  <title>Non-Leased Drivers List</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      padding: 40px;
      background: #f0f2f5;
      color: #333;
    }

    h1 {
      text-align: center;
      color: #2c3e50;
      margin-bottom: 40px;
      font-size: 32px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background-color: white;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
      border-radius: 12px;
      overflow: hidden;
    }

    th, td {
      padding: 16px;
      border-bottom: 1px solid #e0e0e0;
      text-align: left;
      font-size: 16px;
    }

    th {
      background-color: #f7f7f7;
      font-weight: 600;
    }

    tr:hover {
      background-color: #fafafa;
    }

    img {
      height: 60px;
      width: 60px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid #ccc;
    }

    .profile-btn {
      background-color: #3498db;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s ease;
    }

    .profile-btn:hover {
      background-color: #2980b9;
    }

    form {
      margin: 0;
    }

    td:last-child {
      text-align: center;
    }
  </style>
</head>
<body>

  <h1>Non-Lease Drivers</h1>

  <table>
    <thead>
      <tr>
        <th>Username</th>
        <th>Vehicle Number</th>
        <th>Profile Photo</th>
        <th>Profile</th>
      </tr>
    </thead>
    <tbody>
      <% nonLeasedDrivers.forEach(driver => { %>
        <tr>
             <td>
            <% if (driver.photo) { %>
              <img src="<%= driver.photo %>" alt="Photo">
            <% } else { %>
              No Photo
            <% } %>
          </td>
          <td><%= driver.username %></td>
          <td><%= driver.vehicle %></td>
          <td>
            <form action="/admin-view-nonlease-driver-profile" method="GET">
              <input type="hidden" name="username" value="<%= driver.username %>">
              <button type="submit" class="profile-btn">Go to Profile</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
