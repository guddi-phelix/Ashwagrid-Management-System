<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Driver Profile – Leased</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    /* Global Styles */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #e9f7ff, #fff1f8);
      color: #333;
      min-height: 100vh;
    }

    /* Top Bar */
    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background-color: #fff;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
      flex-wrap: wrap; /* ✅ helps wrap elements on small screens */
    }

    .top-bar h1 {
      font-size: 28px;
      color: #007bff;
    }

    .logout-btn {
      background-color: #dc3545;
      color: white;
      border: none;
      padding: 10px 18px;
      font-size: 18px;
      border-radius: 30px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .logout-btn:hover {
      background-color: #c82333;
    }

    /* Sidebar */
    .sidebar {
      position: fixed;
      top: 120px;
      left: 20px;
      display: flex;
      flex-direction: column;
      gap: 15px;
      z-index: 100;
    }

    .sidebar button {
      font-family: 'Poppins', sans-serif;
      font-size: 16px;
      padding: 12px 20px;
      border-radius: 8px;
      border: none;
      width: 210px;
      text-align: left;
      cursor: pointer;
      white-space: nowrap;
      border-right: 3px solid #ccc;
      transition: all 0.3s ease;
      color: white;
      background-color: #007bff;
    }

    .sidebar button:hover {
      background-color: #0056b3;
      transform: translateX(5px);
    }

    .sidebar button:nth-child(2) {
      background-color: #28a745;
    }

    .sidebar button:nth-child(2):hover {
      background-color: #1e7e34;
    }

    /* Profile Container */
    .profile-container {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 5px 25px rgba(0, 0, 0, 0.08);
    }

    .profile-header {
      display: flex;
      align-items: center;
      gap: 40px;
      margin-bottom: 30px;
      border-bottom: 1px solid #eee;
      padding-bottom: 30px;
      flex-wrap: wrap; /* ✅ makes profile header adjust on small screens */
    }

    .profile-pic {
      width: 180px;
      height: 180px;
      border-radius: 50%;
      object-fit: cover;
      border: 5px solid #007bff;
      box-shadow: 0 0 12px rgba(0, 123, 255, 0.3);
    }

    .profile-info {
      flex: 1;
    }

    .profile-info h2 {
      font-size: 28px;
      color: #007bff;
      margin-bottom: 10px;
    }

    .info {
      font-size: 18px;
      margin: 8px 0;
    }

    .label {
      font-weight: 600;
      color: #555;
    }

    /* Trip Select Row */
    .trip-select-row {
      display: flex;
      align-items: center;
      gap: 20px;
      margin-top: 40px;
      background: #f1f9ff;
      padding: 20px;
      border-radius: 16px;
      box-shadow: 0 4px 14px rgba(0, 123, 255, 0.1);
      border: 1px solid #d0e7ff;
      flex-wrap: wrap; /* ✅ allows select to move down on mobile */
    }

    .trip-select-row label {
      font-size: 18px;
      font-weight: 600;
      color: #007bff;
      white-space: nowrap;
    }

    .trip-select-row select {
      flex: 1;
      font-size: 16px;
      padding: 12px 16px;
      border-radius: 12px;
      border: 2px solid #007bff;
      background: #fff;
      color: #333;
      box-shadow: 0 2px 8px rgba(0, 123, 255, 0.05);
      transition: border-color 0.3s ease;
      font-family: 'Poppins', sans-serif;
    }

    .trip-select-row select:focus {
      border-color: #0056b3;
      outline: none;
    }

    /* ✅ Sidebar Responsive Fix */
    @media (max-width: 992px) {
      .sidebar {
        position: static;
        flex-direction: row;
        justify-content: center;
        gap: 12px;
        margin: 20px auto;
        width: 100%;
      }

      .sidebar button {
        width: auto;
        padding: 10px 15px;
        font-size: 14px;
        border-right: none;
      }

      .profile-container {
        margin: 20px;
        padding: 25px;
      }

      .profile-pic {
        width: 140px;
        height: 140px;
      }

      .profile-info h2 {
        font-size: 22px;
      }

      .info {
        font-size: 16px;
      }
    }
    /* Main Content with Sidebar + Profile */
.main-content {
  display: flex;
  gap: 30px;   /* space between sidebar and profile */
  margin: 40px auto;
  max-width: 1200px;
  padding: 0 20px;
}

/* Sidebar aligned left */
.sidebar {
  gap: 15px;
  min-width: 200px;
}

.sidebar button {
  font-family: 'Poppins', sans-serif;
  font-size: 16px;
  padding: 12px 20px;
  border-radius: 8px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  color: white;
  background-color: #007bff;
}

.sidebar button:hover {
  background-color: #0056b3;
}

.sidebar button:nth-child(2) {
  background-color: #28a745;
}

.sidebar button:nth-child(2):hover {
  background-color: #1e7e34;
}

/* Profile box stays on right */
.profile-container {
  flex: 1;
  background: white;
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 5px 25px rgba(0, 0, 0, 0.08);
}
/* Main Content Layout */
.main-content {
  display: flex;
  align-items: flex-start;
  gap: 40px;                /* space between sidebar and profile */
  margin: 40px auto;
  max-width: 1200px;        /* total layout width */
  padding: 0 20px;
}

/* Sidebar stays small */
.sidebar {
  display: flex;
  flex-direction: column;
  gap: 20px;
  flex: 0 0 200px;          /* fixed width */
}

/* Profile container takes the rest */
.profile-container {
  flex: 1;                  /* expand to fill remaining width */
  min-width: 600px;         /* ✅ ensures it's not too narrow */
  background: white;
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 5px 25px rgba(0, 0, 0, 0.08);
}


  </style>

  <script>
    function goToForm(select) {
      const link = select.value;
      if (link) {
        window.location.href = link;
      }
    }
  </script>
</head>
<body>
  <!-- Top Bar -->
  <div class="top-bar">
    <h1>Driver Profile</h1>
    <form action="/logout" method="POST">
      <button type="submit" class="logout-btn">Logout</button>
    </form>
  </div>

  <!-- Main Content Layout -->
  <div class="main-content">
    
    <!-- Sidebar -->
    <div class="sidebar">
      <button onclick="location.href='/driver_daily_accounts'">Daily Accounts</button>
      <button onclick="location.href='/variable-performance'">Variable Performance</button>
    </div>

    <!-- Profile Section -->
    <div class="profile-container">
      <div class="profile-header">
        <% if (profilePhoto) { %>
          <img src="<%= profilePhoto %>" alt="Profile Photo" class="profile-pic">
        <% } else { %>
          <img src="https://via.placeholder.com/180" alt="No Photo" class="profile-pic">
        <% } %>

        <div class="profile-info">
          <h2><%= username %></h2>
          <h2><%= agdId %></h2>
          <div class="info"><span class="label">Vehicle Number:</span> <%= vehicleNumber %></div>
          <div class="info"><span class="label">Status:</span> Leased</div>
        </div>
      </div>

      <div class="trip-select-row">
        <label for="trip-select">📝 Choose Trip Form Type:</label>
        <select id="trip-select" onchange="goToForm(this)">
          <option value="">-- Select Trip Type --</option>
          <option value="/trip-form/before">Before Start</option>
          <option value="/trip-form/cng">Filling CNG</option>
          <option value="/trip-form/after">After End</option>
        </select>
      </div>
    </div>
  </div>
</body>

</html>
