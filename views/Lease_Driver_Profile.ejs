<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Driver Profile – Leased</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #e9f7ff, #fff1f8);
      color: #333;
      min-height: 100vh;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background-color: #fff;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
    }

    .top-bar h1 {
      font-size: 28px;
      color: #007bff;
    }

    .logout-btn {
      background-color: #dc3545;
      color: white;
      border: none;
      padding: 10px 18px;
      font-size: 20px;
      border-radius: 30px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .logout-btn:hover {
      background-color: #c82333;
    }

    .profile-container {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 5px 25px rgba(0, 0, 0, 0.08);
    }

    .profile-header {
      display: flex;
      align-items: center;
      gap: 40px;
      margin-bottom: 30px;
      border-bottom: 1px solid #eee;
      padding-bottom: 30px;
    }

    .profile-pic {
      width: 180px;
      height: 180px;
      border-radius: 50%;
      object-fit: cover;
      border: 5px solid #007bff;
      box-shadow: 0 0 12px rgba(0, 123, 255, 0.3);
    }

    .profile-info {
      flex: 1;
    }

    .profile-info h2 {
      font-size: 32px;
      color: #007bff;
      margin-bottom: 15px;
    }

    .info {
      font-size: 18px;
      margin: 10px 0;
    }

    .label {
      font-weight: 600;
      color: #555;
    }

    label {
      font-weight: 600;
      font-size: 18px;
      margin-bottom: 10px;
      display: block;
    }

    select {
      width: 100%;
      font-size: 16px;
      padding: 12px;
      border-radius: 10px;
      border: 1px solid #ccc;
      margin-top: 8px;
      background-color: #fdfdfd;
      transition: border-color 0.3s ease;
    }

    select:focus {
      border-color: #007bff;
      outline: none;
    }
    select, select option {
  font-family: 'Poppins', sans-serif;
  font-size: 16px; /* you can adjust size as needed */
}
.trip-select-row {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-top: 40px;
  background: #f1f9ff;
  padding: 20px;
  border-radius: 16px;
  box-shadow: 0 4px 14px rgba(0, 123, 255, 0.1);
  border: 1px solid #d0e7ff;
}

.trip-select-row label {
  font-size: 20px;
  font-weight: 600;
  color: #007bff;
  white-space: nowrap;
}

.trip-select-row select {
  flex: 1;
  font-size: 17px;
  padding: 12px 16px;
  border-radius: 12px;
  border: 2px solid #007bff;
  background: #fff;
  color: #333;
  box-shadow: 0 2px 8px rgba(0, 123, 255, 0.05);
  transition: border-color 0.3s ease;
}

.trip-select-row select:focus {
  border-color: #0056b3;
  outline: none;
}

  </style>

  <script>
    function goToForm(select) {
      const link = select.value;
      if (link) {
        window.location.href = link;
      }
    }
  </script>
</head>
<body>

  <!-- Top Bar with Logout -->
  <div class="top-bar">
    <h1>Driver Profile</h1>
    <form action="/logout" method="POST">
      <button type="submit" class="logout-btn">Logout</button>
    </form>
  </div>

  <div class="profile-container">
    <div class="profile-header">
      <% if (profilePhoto) { %>
        <img src="<%= profilePhoto %>" alt="Profile Photo" class="profile-pic">
      <% } else { %>
        <img src="https://via.placeholder.com/180" alt="No Photo" class="profile-pic">
      <% } %>

      <div class="profile-info">
        <h2><%= username %></h2>
        <div class="info"><span class="label">Vehicle Number:</span> <%= vehicleNumber %></div>
        <div class="info"><span class="label">Status:</span> Leased</div>
      </div>
    </div>

  <div class="trip-select-row">
  <label for="trip-select">📝 Choose Trip Form Type:</label>
  <select id="trip-select" onchange="goToForm(this)">
    <option value="">-- Select Trip Type --</option>
    <option value="/trip-form/before">Before Start</option>
    <option value="/trip-form/cng">Filling CNG</option>
    <option value="/trip-form/after">After End</option>
  </select>
</div>

  </div>

</body>
</html>
